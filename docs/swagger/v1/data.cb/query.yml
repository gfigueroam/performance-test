post:
  summary: Queries all calculated behavior data by key
  description: |
    Gets a list of calculated behavior data stored under any key matching the query prefix. [More](/docs/method-data.cb.query.html)
  tags:
    - Data.CB
  security:
    - UserServiceSIF: []
  parameters:
    - name: cb
      in: body
      required: true
      schema:
        type: object
        properties:
          keyPrefix:
            type: string
          requestor:
            type: string
          owner:
            type: string
        required:
          - keyPrefix
  responses:
    200:
      description: Returns a list of stored JSON documents that match the key query prefix.
      schema:
        type: object
        properties:
          ok:
            type: boolean
          result:
            type: array
            items:
              type: object
              properties:
                createdBy:
                  type: string
                key:
                  type: string
                data:
                  type: string
                app:
                  type: string
                updatedBy:
                  type: string
              required:
                - createdBy
                - key
                - data
                - app
        required:
          - ok
          - result
